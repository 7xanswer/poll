{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <div class="container d-flex align-items-center h-100">
        <div class="row">
            <div class="col-md-12">
                <a href="{{ path('user_index') }}">Retour aux profil</a>
                <h1>Modifier</h1>
                <div class="mb-3">
                    <p class="m-0">Mettez à jour les informations de votre profil.</p>
                </div>
                {{ include('user/_form.html.twig', {'button_label': 'Update'}) }}
                {{ include('user/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
