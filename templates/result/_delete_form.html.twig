<form method="post" action="{{ path('result_delete', {'id': result.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ result.id) }}">
    <button class="btn">Delete</button>
</form>
