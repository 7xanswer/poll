{% extends 'base.html.twig' %}

{% block title %}Edit Answer{% endblock %}

{% block body %}
    <div class="container d-flex align-items-center h-100">
        <div class="row">
            <div class="col-md-12">
                <a href="{{ path('answer_index') }}">Retour aux réponses</a>
                <h1>Modifier</h1>
                <div class="mb-3">
                    <p class="m-0">Mettez à jour les informations de votre réponse.</p>
                </div>
                {{ include('answer/_form.html.twig', {'button_label': 'Update'}) }}
                {{ include('answer/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
    <h1>Edit Answer</h1>
{% endblock %}
